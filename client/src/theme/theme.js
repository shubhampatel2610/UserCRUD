import { createTheme } from "@mui/material";

const theme = createTheme({
  primary: {
    headerBgColor: "#000000",
    blackColor: "#000000",
    whiteColor: "#ffffff",
    whiteBgColor: "#ffffff",
    headerFontColor: "#ffffff",
    boxShadowColor: "#00000032",
    buttonColor: "#9A4848",
    buttonHoverColor: "#9A5656",
    borderColor: "#BABABA",
    placeholderColor: " rgba(0, 0, 0, 0.85);",
    greenColor: "green",
    blueColor: "blue",
    redColor: "red",
  },
});

export default theme;
